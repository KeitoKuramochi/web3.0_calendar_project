# Issue #8: 全体統合・ダッシュボード・README整備 (docs/readme)

## 📌 概要
トップページ（ダッシュボード）を作成し、これまで作ったプロフィール設定画面、相談予約リクエスト画面、マッチング・スコアリング画面、メール生成・チェック画面を有機的に連結します。また、起動手順などをまとめた `README.md` を最終更新し、動作検証を行います。

## 🎯 ゴール
- `src/app/page.tsx` (トップページ) を実装し、以下の構成でダッシュボードを構築すること：
  - アプリの基本情報とクイックスタートカード（「プロフィールを更新する」「新しい相談を予約する」）
  - 「進行中の調整状況」のダミータイムライン（プロトタイプの体験性を上げるため）
- 画面間の遷移が Navbar やボタンを通じてスムーズに行えること
- `README.md` に推奨技術、フォルダ構成、起動方法、各モックAIの仕様、GitHub/Gitブランチの運用方法を初心者にもわかりやすく記述すること
- `npm run build` を通して、エラーが出ずにビルドが完了すること

## 🛠️ 作業手順
1. `src/app/page.tsx` および `src/app/page.module.css` (必要に応じて) を作成する
2. ダッシュボードのレイアウト（クイックリンク、タイムライン、統計情報）の構築
3. `README.md` の記述・最終更新
4. ローカルビルドテスト (`npm run build`) の実行
5. 変更をコミットし、`main` にマージする
