# MVP Tasks

> このファイルはPlannerが生成し、GeneratorとEvaluatorが更新します。

## ステータス凡例

- `[ ]` 未着手
- `[>]` 実装中（Generator作業中）
- `[x]` 完了（Evaluator合格済み）
- `[?]` Evaluator確認待ち
- `[!]` 不合格（Evaluatorが戻した、修正中）

---

## Phase 1 — ダミーデータでUIを動かす

### TASK-001: プロジェクト初期セットアップ

- **説明**: Cloudflare Pages（Astro + React）と Cloudflare Workers（Hono）の2リポジトリ構成をモノレポでセットアップし、開発サーバーが起動することを確認する。
- **完了条件**:
  - [ ] `npm run dev` を実行するとフロントエンド（Astro）が `localhost:4321` で起動する
  - [ ] `npm run dev:api` を実行するとWorkers（Hono）が `localhost:8787` で起動する
  - [ ] `localhost:4321` にアクセスするとAstroのデフォルトページまたは空のページが表示される
  - [ ] `npm run build` がエラーなく完了する
- **ステータス**: `[x]`
- **commit**: c2289e5
- **Evaluator確認**: 合格 (2026-06-08)

---

### TASK-002: ランディングページ（ログイン前）

- **説明**: `/` にアクセスしたとき表示されるランディングページを実装する。プロダクトの説明文とGoogleログインボタン（ダミー）を配置する。
- **完了条件**:
  - [ ] `localhost:4321/` にアクセスするとランディングページが表示される
  - [ ] プロダクト名・サービス説明文（1〜3文）が表示される
  - [ ] 「Googleでログイン」ボタンが表示される
  - [ ] 「Googleでログイン」ボタンをクリックすると `/onboarding` へ遷移する（Phase 1はダミー遷移でよい）
  - [ ] `npm run build` がエラーなく完了する
- **ステータス**: `[x]`
- **commit**: 55ca97d
- **自己評価**: プロダクト名・サービス説明文（3文）・GoogleロゴSVG付き「Googleでログイン」ボタンを実装。`<a href="/onboarding">` でダミー遷移。npm run build 通過確認済み。
- **Evaluator確認**: 合格 (2026-06-08)

---

### TASK-003: ロール選択画面（オンボーディング）

- **説明**: `/onboarding` に「先生」「学生」のどちらかを選ぶ画面を実装する。選択後のダミー遷移先も設定する。
- **完了条件**:
  - [ ] `localhost:4321/onboarding` にアクセスするとロール選択画面が表示される
  - [ ] 「先生として始める」「学生として始める」の2つの選択肢が表示される
  - [ ] 「先生として始める」をクリックすると `/teacher` へ遷移する
  - [ ] 「学生として始める」をクリックすると `/student` へ遷移する
  - [ ] `npm run build` がエラーなく完了する
- **ステータス**: `[x]`
- **commit**: 04444f0
- **自己評価**: `/onboarding` にロール選択画面を実装。「先生として始める」→/teacher、「学生として始める」→/student へ遷移するaタグを配置。Tailwind CSSでシンプルにスタイリング。npm run build 通過確認済み。
- **Evaluator確認**: 合格 (2026-06-08)

---

### TASK-004: 先生ダッシュボード UI（カレンダー・承認待ち・課題）

- **説明**: `/teacher` に先生ダッシュボードをダミーデータで実装する。週ビューカレンダー・承認待ちリスト・学生課題一覧の3セクションを配置する。
- **完了条件**:
  - [ ] `localhost:4321/teacher` にアクセスすると先生ダッシュボードが表示される
  - [ ] 週ビューのカレンダーが表示され、ダミーの空き枠（3件以上）が色付きで表示されている
  - [ ] 「承認待ちリスト」セクションにダミーの面談リクエスト（2件以上）が表示されている
  - [ ] 各リクエストに「承認」「差し戻し」ボタンが表示されている（クリックは未実装でよい）
  - [ ] 「学生課題一覧」セクションにダミーの学生名・課題名・ステータスが表示されている
  - [ ] `/chat` へ遷移する「AIに話しかける」ボタンまたはリンクが表示されている
  - [ ] `npm run build` がエラーなく完了する
- **ステータス**: `[x]`
- **commit**: 176d68c
- **自己評価**: `/teacher` に先生ダッシュボードを実装。週ビューカレンダー（今週月〜日の7列、9〜17時の時間帯）にダミー空き枠3件を緑色で表示。承認待ちリスト2件（学生名・希望日時・承認ボタン緑・差し戻しボタン赤）。学生課題一覧3件（学生名・課題名・ステータスバッジ）。ヘッダーと下部に「AIに話しかける」ボタン（href="/chat"）を配置。npm run build 通過確認済み。
- **Evaluator確認**: 合格 (2026-06-08)

---

### TASK-005: 学生ダッシュボード UI（カレンダー・課題・チャット導線）

- **説明**: `/student` に学生ダッシュボードをダミーデータで実装する。先生の空き枠カレンダー・自分の課題一覧・チャット導線の3セクションを配置する。
- **完了条件**:
  - [ ] `localhost:4321/student` にアクセスすると学生ダッシュボードが表示される
  - [ ] 先生の空き枠を示す週ビューカレンダーが表示され、ダミーの空き枠（3件以上）が表示されている
  - [ ] 「自分の課題」セクションにダミーの課題名・期限・ステータスが表示されている
  - [ ] 「相談する（チャットへ）」ボタンが表示され、クリックすると `/chat` へ遷移する
  - [ ] `npm run build` がエラーなく完了する
- **ステータス**: `[x]`
- **commit**: 7fbe7cb
- **自己評価**: `/student` に学生ダッシュボードを実装。先生の空き枠週ビューカレンダー（今週月〜日の7列、9〜17時の時間帯）にダミー空き枠3件（火・木・土曜）を緑色で表示。自分の課題一覧3件（課題名・期限・ステータスバッジ）。ヘッダーと下部に「相談する（チャットへ）」ボタン（href="/chat"）を配置。npm run build 通過確認済み。
- **Evaluator確認**: 合格 (2026-06-08)

---

### TASK-006: チャット画面 UI（エコーbot）

- **説明**: `/chat` にチャット画面を実装する。Phase 1はAI接続なしで、入力したメッセージをそのままエコー返信するbotとして動作させる。
- **完了条件**:
  - [ ] `localhost:4321/chat` にアクセスするとチャット画面が表示される
  - [ ] メッセージ入力欄と「送信」ボタンが表示されている
  - [ ] テキストを入力して「送信」ボタンをクリックすると、自分のメッセージが吹き出しで表示される
  - [ ] 送信後1〜2秒以内にエコーbot（「AIボット」表示）が同じ内容を返信する吹き出しが表示される
  - [ ] 複数回送受信してもメッセージが上から時系列に並ぶ
  - [ ] `npm run build` がエラーなく完了する
- **ステータス**: `[x]`
- **commit**: ca4864b
- **自己評価**: `/chat` にチャット画面を実装。`frontend/src/components/Chat.tsx`（Reactエコーbot）と `frontend/src/pages/chat.astro` を新規作成。自分のメッセージは右寄せ青吹き出し、AIボットのメッセージは左寄せグレー吹き出し（「AIボット」ラベル付き）。送信後1000msでエコー返信。タイピングアニメーション表示。Enterキーでも送信可。複数回送受信でメッセージが時系列に追加される。npm run build 通過確認済み。
- **Evaluator確認**: 合格 (2026-06-08)

---

## Phase 2 — インフラ接続

### TASK-007: Cloudflare D1 + Drizzle ORM セットアップ

- **説明**: Cloudflare D1データベースを作成し、Drizzle ORMでスキーマ（users・groups・groupMembers・chatLog・memory）を定義してマイグレーションを実行する。
- **完了条件**:
  - [ ] `wrangler d1 create` でD1データベースが作成されている
  - [ ] Drizzleスキーマに5テーブル（users・groups・groupMembers・chatLog・memory）が定義されている
  - [ ] `drizzle-kit migrate` がエラーなく完了し、D1にテーブルが作成されている
  - [ ] Honoのルートから `db.select()` でusersテーブルを照会すると空配列が返る
  - [ ] `npm run build` がエラーなく完了する
- **ステータス**: `[?]` 完了（Evaluator確認待ち）
- **commit**: 0827900
- **自己評価**: wrangler.tomlにD1バインディング追加。api/src/db/schema.tsに5テーブル（users・groups・groupMembers・chatLog・memory）定義。drizzle-kit generateでmigrations/0000_volatile_the_call.sql生成。wrangler d1 execute --localでローカルD1にテーブル作成（9 commands executed）。api/src/index.tsに/db-checkルート追加（drizzle select from users）。npm run build 通過確認済み（wrangler出力でenv.DB D1 Database確認）。
- **Evaluator確認**: 未

---

### TASK-008: Google OAuth 認証

- **説明**: Google OAuthでログイン・コールバック・セッション管理を実装する。Cloudflare KVにセッションを保存し、ログイン状態に応じて画面を出し分ける。
- **完了条件**:
  - [ ] `localhost:4321/` の「Googleでログイン」ボタンをクリックするとGoogleのOAuth同意画面に遷移する
  - [ ] Googleアカウントで同意するとコールバック後に `/onboarding` または `/teacher` or `/student` へリダイレクトされる
  - [ ] ログイン後、画面右上にGoogleアカウントの名前またはアバターが表示される
  - [ ] ログアウトボタンをクリックするとセッションが削除され、`/` にリダイレクトされる
  - [ ] ログイン済みのまま `/` にアクセスするとダッシュボードへリダイレクトされる
  - [ ] `npm run build` がエラーなく完了する
- **ステータス**: `[?]` 完了（Evaluator確認待ち）
- **commit**: 3a80551
- **自己評価**: Hono側にGoogleOAuthフロー（/auth/login, /auth/callback, /auth/me, /auth/logout）を実装。jose を使った署名付きJWT cookie でセッション管理。astro.config.mjs に /api プロキシ設定追加。AuthGuard（ダッシュボード用）・HomeAuthGuard（ランディング用リダイレクト含む）Reactコンポーネントを作成し各ページに配置。「Googleでログイン」ボタンを /api/auth/login へ変更。全fetchリクエストに credentials:include を追加。npm run build 通過確認済み。
- **Evaluator確認**: 未

---

### TASK-009: オンボーディング（研究室作成・参加コード）

- **説明**: 初回ログイン後のロール選択・研究室作成・参加コード発行・参加コードによる研究室参加をD1に保存して実装する。
- **完了条件**:
  - [ ] 初回ログイン後 `/onboarding` でロール選択画面が表示される
  - [ ] 「先生として始める」を選ぶと `/onboarding/create` で研究室名入力フォームが表示される
  - [ ] 研究室名を入力して「作成」ボタンをクリックすると、8文字程度の参加コードが画面に表示される
  - [ ] 「学生として始める」を選ぶと `/onboarding/join` で参加コード入力フォームが表示される
  - [ ] 正しい参加コードを入力して「参加」ボタンをクリックすると、`/student` へ遷移する
  - [ ] 誤った参加コードを入力すると「参加コードが正しくありません」というエラーメッセージが表示される
  - [ ] `npm run build` がエラーなく完了する
- **ステータス**: `[?]` 完了（Evaluator確認待ち）
- **commit**: 93c1fa0
- **自己評価**: api/src/index.ts に POST /groups/create（研究室作成・joinCode発行・usersロールをteacherに更新）と POST /groups/join（参加コード検証・usersロールをstudentに更新・404でinvalid_codeエラー）を追加。frontend/src/components/Onboarding.tsx を新規作成（step: role/teacher-create/student-join/teacher-doneの4ステート管理、研究室名入力→作成→参加コード表示、参加コード入力→参加→/studentへ遷移、誤コードで「参加コードが正しくありません」エラー表示）。frontend/src/pages/onboarding.astro を `<Onboarding client:load />` に更新。npm run build 通過確認済み。
- **Evaluator確認**: 未

---

### TASK-010: 先生の空き枠管理（D1保存・カレンダー反映）

- **説明**: 先生が先生ダッシュボードのカレンダーUIから空き枠を追加・削除し、D1に保存してリロード後も反映される状態にする。
- **完了条件**:
  - [ ] `localhost:4321/teacher` のカレンダーで空いているセルをクリックすると、日時入力ダイアログが表示される
  - [ ] 日時を確認して「追加」ボタンをクリックすると、カレンダーに新しい空き枠が表示される
  - [ ] ページをリロードしても追加した空き枠が表示されている
  - [ ] 空き枠をクリックして「削除」ボタンをクリックすると、その枠がカレンダーから消える
  - [ ] `npm run build` がエラーなく完了する
- **ステータス**: `[x]`
- **commit**: 9241f87
- **自己評価**:
  - api/src/db/schema.ts に slots テーブル追加。drizzle-kit generate で 0001_nifty_cyclops.sql 生成、wrangler d1 execute --local で適用完了。
  - GET /slots（自分のスロット一覧）・POST /slots（スロット追加）・DELETE /slots/:id（スロット削除・本人確認付き）を index.ts に追加。
  - TeacherDashboard.tsx を新規作成。マウント時に GET /api/slots でD1から取得、空きセルクリックで追加ダイアログ表示→「追加」で POST、緑セルクリックで削除ダイアログ表示→「削除」で DELETE を実行し再描画する。
  - teacher.astro を `<TeacherDashboard client:load />` に置き換え。承認待ちリスト・課題一覧はダミーデータのまま。
  - npm run build 成功確認済み。
- **Evaluator確認**: 合格 (2026-06-08)

---

### TASK-011: 面談リクエスト送信（学生側）

- **説明**: 学生が先生の空き枠を選択して面談リクエストをD1に保存し、先生の承認待ちリストに表示されるようにする。
- **完了条件**:
  - [ ] `localhost:4321/student` のカレンダーで先生の空き枠をクリックすると「この枠でリクエストする」ボタンが表示される
  - [ ] ボタンをクリックすると確認ダイアログが表示される
  - [ ] 確認ダイアログで「送信」をクリックすると、学生ダッシュボードに「承認待ち」ステータスの面談が表示される
  - [ ] 先生が `localhost:4321/teacher` を確認すると、承認待ちリストに当該リクエストが表示されている
  - [ ] `npm run build` がエラーなく完了する
- **ステータス**: `[?]` 完了（Evaluator確認待ち）
- **commit**: 9a029d2
- **自己評価**:
  - meetingRequests テーブルを schema.ts に追加。drizzle-kit generate で 0002_ancient_devos.sql 生成、wrangler d1 execute --local で適用完了。
  - GET /teacher/slots（同グループの先生の空き枠を学生が取得）・POST /meeting-requests（学生から面談リクエスト送信、学生ロール必須）・GET /meeting-requests（先生は自分宛のリクエスト、学生は自分が送ったリクエストを取得）を index.ts に追加。
  - StudentDashboard.tsx を新規作成（先生の空き枠カレンダー・空き枠クリックでリクエストボタン表示ダイアログ→「この枠でリクエストする」クリックで確認ダイアログ→「送信」でPOST /meeting-requests→面談リクエスト一覧に「承認待ち」表示）。
  - TeacherDashboard.tsx の承認待ちリストを GET /meeting-requests で取得したD1データに切り替え（PENDING_REQUESTSダミーデータ削除）。
  - student.astro を `<StudentDashboard client:load />` に置き換え。
  - npm run build 成功確認済み。
- **Evaluator確認**: 未

---

### TASK-012: 面談リクエスト承認・差し戻しフロー（先生側）

- **説明**: 先生が承認待ちリストのリクエストを承認または差し戻し（代替日程提示）でき、結果が学生ダッシュボードに反映されるようにする。
- **完了条件**:
  - [ ] 先生ダッシュボードの承認待ちリストで「承認」ボタンをクリックすると、そのリクエストのステータスが「確定済み」に変わる
  - [ ] 学生ダッシュボードで確定した面談の日時が表示される
  - [ ] 先生ダッシュボードの承認待ちリストで「差し戻し」ボタンをクリックすると、代替日程入力フォームが表示される
  - [ ] 代替日程を入力して「差し戻す」ボタンをクリックすると、学生ダッシュボードに代替案が表示され「選択待ち」ステータスになる
  - [ ] 学生が代替案を選択すると、先生ダッシュボードに再度「承認待ち」で表示される
  - [ ] `npm run build` がエラーなく完了する
- **ステータス**: `[ ]`
- **commit**: —
- **Evaluator確認**: 未

---

### TASK-013: 課題管理（割り当て・完了報告）

- **説明**: 先生が学生に課題を割り当て、学生が完了報告できる機能をD1に保存して実装する。
- **完了条件**:
  - [ ] 先生ダッシュボードに「課題を追加」ボタンが表示され、クリックすると対象学生・課題名・期限の入力フォームが表示される
  - [ ] フォームを送信すると先生ダッシュボードの学生課題一覧に新しい課題が追加される
  - [ ] 学生ダッシュボードの「自分の課題」に同じ課題が表示される
  - [ ] 学生が「完了報告」ボタンをクリックすると課題のステータスが「完了」に変わる
  - [ ] 先生ダッシュボードでも同じ課題のステータスが「完了」に変わっている
  - [ ] `npm run build` がエラーなく完了する
- **ステータス**: `[ ]`
- **commit**: —
- **Evaluator確認**: 未

---

## Phase 3 — bot知能化

### TASK-014: Gemini Flash 2.0 チャット接続

- **説明**: チャット画面のエコーbotをGemini Flash 2.0に差し替える。メッセージ送信後にGeminiからの返答がストリーミング表示される。
- **完了条件**:
  - [ ] `localhost:4321/chat` でメッセージを送信するとGeminiからの返答が表示される
  - [ ] 返答がストリーミング（文字が順番に表示される）で描画される、またはローディング後に一括表示される
  - [ ] 「こんにちは」「進捗を報告したい」などの一般的な入力に対してGeminiが文脈に沿った返答をする
  - [ ] `npm run build` がエラーなく完了する
- **ステータス**: `[ ]`
- **commit**: —
- **Evaluator確認**: 未

---

### TASK-015: chatLog永久保存 + memory自動更新

- **説明**: チャットの全会話をchatLogテーブルに保存し、会話終了（5分無操作などのトリガー）後にGeminiがmemoryを自動更新するようにする。
- **完了条件**:
  - [ ] チャットでメッセージを送受信するたびにD1のchatLogテーブルにレコードが追加されている（Wranglerコンソールで確認可能）
  - [ ] チャット画面をリロードすると直近の会話履歴（最低5件）が表示される
  - [ ] チャット終了から一定時間後（または「会話を終了する」ボタン押下後）、D1のmemoryテーブルにそのユーザーのmemoryレコードが更新される
  - [ ] 次の会話開始時に、前回のmemoryの内容をGeminiが参照した発言（「前回は〜でしたね」など）をすることがある
  - [ ] `npm run build` がエラーなく完了する
- **ステータス**: `[ ]`
- **commit**: —
- **Evaluator確認**: 未

---

### TASK-016: 先生チャット — 自然言語で空き枠設定

- **説明**: 先生がチャットで「来週火曜午後は空いてる」と入力すると、Geminiが日時を解釈して空き枠をD1に自動登録し、カレンダーに反映させる。
- **完了条件**:
  - [ ] 先生が `/chat` で「来週火曜の14時から17時は空き」と入力すると、Geminiが「来週火曜14:00〜17:00を空き枠として登録しました」という旨の返答をする
  - [ ] `/teacher` のカレンダーを確認すると、Geminiが解釈した日時に空き枠が追加されている
  - [ ] 「月曜の空き枠を削除して」と入力すると、該当する空き枠がカレンダーから消える
  - [ ] 曖昧な表現（「来週後半のどこか」）に対してGeminiが「具体的な日時を教えてください」と聞き返す
  - [ ] `npm run build` がエラーなく完了する
- **ステータス**: `[ ]`
- **commit**: —
- **Evaluator確認**: 未

---

### TASK-017: 学生チャット — 相談分岐（bot解決 or 面談準備）

- **説明**: 学生がチャットで相談すると、Geminiがbot解決できるかを判断し、できる場合はそのまま解決し、面談が必要な場合は準備会話を始めて面談リクエスト送信へ誘導する。
- **完了条件**:
  - [ ] 学生が「Pythonのエラーが解決できない」と入力するとGeminiがエラーの解決策を返す（bot解決パス）
  - [ ] 学生が「研究の方向性を相談したい」と入力するとGeminiが「先生との面談が必要そうです。どんな点を相談したいか教えてください」という旨の返答をする（面談準備パス）
  - [ ] 面談準備パスでGeminiが準備質問を2〜3問出し、学生が回答する会話が進む
  - [ ] 準備会話終了後にGeminiが「準備が整いました。希望の日時を先生のカレンダーから選んでください」という旨の案内と、カレンダーへのリンクまたはボタンを表示する
  - [ ] `npm run build` がエラーなく完了する
- **ステータス**: `[ ]`
- **commit**: —
- **Evaluator確認**: 未

---

### TASK-018: Cron通知（Cloudflare Cron + Resend メール）

- **説明**: Cloudflare Cron Triggersで定期的にHono APIを呼び出し、翌日に面談が確定している学生・先生にResendでリマインドメールを送る。
- **完了条件**:
  - [ ] `wrangler.toml` にCronトリガー（毎日8時など）が設定されている
  - [ ] `wrangler dev` でCronをシミュレートすると、翌日に面談が確定しているユーザーのメールアドレス宛にResendが呼ばれる（またはコンソールに送信先・件名が出力される）
  - [ ] `RESEND_API_KEY` 未設定時は「メール送信スキップ」とコンソールに出力されエラーにならない
  - [ ] メール本文に面談の日時・相手の名前が含まれている
  - [ ] `npm run build` がエラーなく完了する
- **ステータス**: `[ ]`
- **commit**: —
- **Evaluator確認**: 未

---

### TASK-019: Workers AI 埋め込み + Cloudflare Vectorize RAG

- **説明**: 過去のchatLogをWorkers AIで埋め込みベクトル化してVectorizeに保存し、チャット時に関連する過去会話を検索してGeminiのコンテキストに追加する。
- **完了条件**:
  - [ ] chatLogに新しいレコードが保存されると、Workers AIでテキストをベクトル化してVectorizeに保存される
  - [ ] `/chat` でメッセージを送信すると、HonoがVectorizeで類似する過去会話を検索してGeminiのプロンプトに追加する
  - [ ] 以前に話した話題（例：「先週のPythonの件」）について聞くと、Geminiが過去会話を参照した返答をする
  - [ ] `npm run build` がエラーなく完了する
- **ステータス**: `[ ]`
- **commit**: —
- **Evaluator確認**: 未

---

### TASK-020: bot学習（自動承認ルール・学生傾向のmemory蓄積）

- **説明**: 先生が「次回から〇〇さんのリクエストは自動承認」と発言するとmemoryに学習し、以降は自動承認する。学生の傾向（好みの時間帯・完遂率）もmemoryに蓄積する。
- **完了条件**:
  - [ ] 先生が `/chat` で「田中さんのリクエストは今後自動承認で」と入力するとGeminiが「承知しました。田中さんのリクエストを自動承認するよう設定しました」と返す
  - [ ] D1のmemoryテーブルを確認すると、先生のmemoryにauto_rulesが追加されている
  - [ ] 田中さんが面談リクエストを送ると、先生の承認待ちリストを経由せずに自動で「確定済み」になる
  - [ ] 学生が面談リクエストを複数回送ると、memoryの好みの時間帯・完遂率が更新される
  - [ ] `npm run build` がエラーなく完了する
- **ステータス**: `[ ]`
- **commit**: —
- **Evaluator確認**: 未
